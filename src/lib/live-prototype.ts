/** Polished landing comps already live on the prior Vercel deploy. */
export const LIVE_PROTOTYPE_ORIGIN = "https://asia-wagyu-growth.vercel.app";

export const livePrototypePaths = {
  vip: `${LIVE_PROTOTYPE_ORIGIN}/landing/vip`,
  gifting: `${LIVE_PROTOTYPE_ORIGIN}/landing/gifting`,
} as const;
